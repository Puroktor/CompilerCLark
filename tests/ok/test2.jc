int i = 2.0;
int b = 0;

string myFunc() {
    return "string";
}

void main() {
    int b = 0;
    string line = readLine();
    int len = length(line);
    double val = parseDouble(line);
    string val2 = myFunc();
}
