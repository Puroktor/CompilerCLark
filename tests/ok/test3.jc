boolean cond() {
    return 2 >= 3;
}

double calc() {
    boolean f = False;
    while(f);
    if (f)
        return 1;
    else {
        if (1 > 2 && cond()) {
            return 0;
        }
        return 3.5;
    }
}


void main() {
    string arr[999];

    int n = parseInt(readLine());
    for (int i=0; i<n; i=i+1) {
        arr[i] = readLine();
    }
    print(arr[0]);

    calc();
}