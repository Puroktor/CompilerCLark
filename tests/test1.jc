int i = 0;
int b = 0;

void main() {
    myFunc();
}

void myFunc() {
    char[5] arr;
    do
    {
    } while(i > 0 && g);
    test(arr);
}

int z = 1 + 1;

int test(char[] arr)
{
    delegate<int[], boolean : void> delegate = func;
    return 2.0;
}
